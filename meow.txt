pip install alembic sqlalchemy psycopg2-binary python-dotenv
alembic init alembic
alembic revision --autogenerate -m "initial users and tasks tables"
alembic upgrade heads
pip install python-jose passlib[bcrypt] python-dotenv
pip freeze > requirements.txt

pip install alembic sqlalchemy psycopg2-binary python-dotenv
alembic init alembic
alembic revision --autogenerate -m "initial users and tasks tables"
alembic upgrade heads
pip install python-jose passlib[bcrypt] python-dotenv
pip freeze > requirements.txt

sudo docker exec -it backup sh